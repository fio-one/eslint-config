{
  "name": "eslint-config-fio",
  "license": "MIT",
  "description": "",
  "homepage": "https://github.com/fio-one/eslint-config#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fio-one/eslint-config.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "publish:latest": "lerna publish --positional=patch --no-git-reset --yes",
    "publish:stable":"lerna publish --positional=patch --no-git-reset --yes",
    "publish:canary":"lerna publish --positional=patch --no-git-reset --yes"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
